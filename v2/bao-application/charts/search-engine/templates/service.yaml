apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-engine.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "search-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "search-engine.selectorLabels" . | nindent 4 }}

